import 'package:flutter/material.dart';

const Widget notFoundMessage = Center(
  child: Text(
    'Content not found! Try Again',
    style: TextStyle(
      color: Colors.white,
      fontSize: 17,
    ),
  ),
);
